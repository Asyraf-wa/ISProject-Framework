@extends('%%layout%%')

@section('title', '%%titleSingular%% details')

@section('content')
    <div class="is-page-header">
        <div>
            <span class="is-page-eyebrow">
                <a href="{{ route('%%routeName%%.index') }}">%%title%%</a>
            </span>
            <h2 class="is-page-title">%%titleSingular%% #{{ $%%modelVariable%%->getKey() }}</h2>
        </div>

        <div class="d-flex gap-2">
            <a href="{{ route('%%routeName%%.edit', $%%modelVariable%%) }}" class="btn btn-primary d-inline-flex align-items-center gap-2">
                <x-isproject::icon name="pencil" /> Edit
            </a>
            <a href="{{ route('%%routeName%%.index') }}" class="btn btn-outline-secondary d-inline-flex align-items-center gap-2">
                <x-isproject::icon name="arrow-left" /> Back
            </a>
        </div>
    </div>

    @include('isproject::partials.alerts')

    <div class="row g-4">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header"><h6 class="mb-0">Details</h6></div>
                <div class="card-body">
                    <dl class="is-detail-list">
%%showRows%%
                    </dl>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card mb-4">
                <div class="card-header"><h6 class="mb-0">Share</h6></div>
                <div class="card-body">
                    {{-- QR sharing hook: swap in <x-isproject::qr :url="url()->current()" /> once the QR module lands. --}}
                    <label class="form-label" for="share-link">Link to this record</label>
                    <input type="text" id="share-link" class="form-control form-control-sm"
                           value="{{ url()->current() }}" readonly onclick="this.select()">
                </div>
            </div>

            <div class="card">
                <div class="card-header"><h6 class="mb-0 text-danger">Danger zone</h6></div>
                <div class="card-body">
                    <p class="small text-body-secondary">
                        Deleting a %%titleSingular%% removes it from every list.
                    </p>
                    <form method="POST" action="{{ route('%%routeName%%.destroy', $%%modelVariable%%) }}"
                          onsubmit="return confirm('Delete this %%titleSingular%%? This cannot be undone.');">
                        @csrf
                        @method('DELETE')
                        <button type="submit" class="btn btn-outline-danger w-100 d-inline-flex align-items-center justify-content-center gap-2">
                            <x-isproject::icon name="trash" /> Delete %%titleSingular%%
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
@endsection
